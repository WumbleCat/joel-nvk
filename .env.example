# Copy to .env and fill in. .env is gitignored; this file is not.

# --- Runtime ---
ENV=dev
LOG_LEVEL=INFO

# --- Paths (optional overrides; defaults are relative to the repo root) ---
DATA_DIR=data
OUTPUTS_DIR=outputs

# --- Secrets ---
# API_KEY=
